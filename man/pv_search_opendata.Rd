% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opendata.R
\name{pv_search_opendata}
\alias{pv_search_opendata}
\title{Search datasets on opendata.swiss}
\usage{
pv_search_opendata(query, rows = 10)
}
\arguments{
\item{query}{Search terms, e.g. \code{"Finanzausgleich Luzern"}.}

\item{rows}{Maximum number of datasets to return (1 to 100).}
}
\value{
A data frame with one row per dataset: \code{title}, \code{id} (the
dataset slug \code{\link[=pv_fetch_opendata]{pv_fetch_opendata()}} takes), \code{organisation} (the
publisher), \code{licence} (the terms its resources are published under),
and \code{formats} (the file formats on offer).
}
\description{
Queries the opendata.swiss catalogue (the Swiss open-government-data
portal) and returns the matching datasets as a compact data frame. Feed
a result's \code{id} to \code{\link[=pv_fetch_opendata]{pv_fetch_opendata()}} to download its data.
}
\examples{
\dontrun{
pv_search_opendata("Finanzausgleich Luzern")
}
}
\seealso{
\code{\link[=pv_fetch_opendata]{pv_fetch_opendata()}}
}
