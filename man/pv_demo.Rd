% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demo.R
\name{pv_demo}
\alias{pv_demo}
\title{Launch the polyviz demo gallery}
\usage{
pv_demo(launch = TRUE)
}
\arguments{
\item{launch}{Run the app right away? \code{TRUE} (default) blocks until
the app is closed, as \code{\link[shiny:runApp]{shiny::runApp()}} does. \code{FALSE} only returns
the app object - the form programmatic drivers want: hand it to
\code{\link[shiny:testServer]{shiny::testServer()}} or a \code{shinytest2::AppDriver} to script the
app without a browser, or to \code{\link[shiny:runApp]{shiny::runApp()}} yourself to pick the
port and host.}
}
\value{
The \code{\link[shiny:shinyApp]{shiny::shinyAppDir()}} app object, invisibly.
}
\description{
A Shiny app that shows every chart family working in one place, on
the bundled Swiss data: a page per family (comparison, distribution,
evolution, composition, relational, geo, motion, tables and
matrices), each chart with live controls for its main options. The
header row switches the theme (packaged or \code{\link[=pv_theme_paper]{pv_theme_paper()}}), the
locale (\code{\link[=pv_locale]{pv_locale()}}), and light/dark mode for every chart at once,
and an always-visible events panel prints the \verb{input$<id>_<event>}
values the charts send back - the Shiny round-trip made visible.
}
\details{
The global controls work by rebuilding the widgets server-side with
\code{\link[=pv_set_theme]{pv_set_theme()}} and \code{\link[=pv_locale]{pv_locale()}} around each build; the app
snapshots and restores the session's own theme and locale around
every build, so running the demo never changes the settings of the
session that launched it.
}
\examples{
if (interactive()) {
  pv_demo()
}

\dontrun{
# Programmatic use: get the app object without launching it, then
# drive it yourself - shiny::testServer, shinytest2, or runApp with
# your own port.
app <- pv_demo(launch = FALSE)
shiny::runApp(app, port = 4321)
}
}
