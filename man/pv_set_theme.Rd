% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-api.R
\name{pv_set_theme}
\alias{pv_set_theme}
\title{Set a session-wide polyviz theme}
\usage{
pv_set_theme(
  colors = NULL,
  colors_dark = NULL,
  sequential = NULL,
  sequential_dark = NULL,
  font = NULL,
  check = TRUE
)
}
\arguments{
\item{colors}{Character vector of categorical colours for light mode,
in the order series will receive them (slot order is part of the
accessibility contract — see \code{\link[=pv_check_palette]{pv_check_palette()}}).}

\item{colors_dark}{Categorical colours for dark mode, same length as
\code{colors}. Omit to derive them from \code{colors} automatically.}

\item{sequential}{Light-mode sequential ramp (low to high, at least 3
colours).}

\item{sequential_dark}{Dark-mode sequential ramp.}

\item{font}{CSS \code{font-family} stack for widget text, e.g.
\code{"Georgia, serif"}.}

\item{check}{Validate the categorical palettes with
\code{\link[=pv_check_palette]{pv_check_palette()}}? \code{TRUE} (default) aborts on hard failure;
\code{FALSE} skips validation with a warning that accessibility is now
your responsibility.}
}
\value{
The full token list now in effect (shaped like \link{pv_colors}),
invisibly.
}
\description{
Replaces the packaged colour tokens for every chart built afterwards
in this session. Pieces you don't supply keep the packaged values, so
\code{pv_set_theme(colors = my_brand)} swaps only the categorical palette
and leaves the sequential ramps, diverging scale, and chart chrome
alone.
}
\details{
When \code{colors} is given without \code{colors_dark}, the dark palette is
derived automatically: each colour keeps its hue and chroma and is
re-stepped into the dark-surface lightness band in OKLCH (desaturated
just enough to stay inside the sRGB gamut).

By default the categorical palettes for both modes must pass
\code{\link[=pv_check_palette]{pv_check_palette()}} — a hard failure aborts with the full report
printed, and pairs in the warning band (Delta E 6–8 under
colour-vision deficiency) come with a warning to pair colour with a
secondary encoding. \code{check = FALSE} skips the gate entirely; polyviz
then makes no accessibility promises about your charts.
}
\examples{
# A four-colour brand palette; the dark twins are derived
pv_set_theme(colors = c("#e07a3f", "#6a4c93", "#7f9032", "#0090ad"))
sales <- aggregate(revenue ~ region, pv_sales, sum)
pv_bar(sales, x = "region", y = "revenue", title = "Now in brand colours")
pv_reset_theme()
}
