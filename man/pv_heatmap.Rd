% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-evolution.R
\name{pv_heatmap}
\alias{pv_heatmap}
\title{Interactive D3 heatmap}
\usage{
pv_heatmap(
  data,
  x,
  y,
  value,
  palette = c("sequential", "diverging"),
  cell_values = "auto",
  truncate_labels = 24,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with at most one row per x/y cell.}

\item{x}{Name of the column mapped to the matrix columns.}

\item{y}{Name of the column mapped to the matrix rows.}

\item{value}{Name of the numeric column mapped to colour.}

\item{palette}{\code{"sequential"} (default, for magnitudes) or
\code{"diverging"} (for values spanning zero, centred there).}

\item{cell_values}{\code{TRUE}, \code{FALSE}, or \code{"auto"} (default). Whether the
cells print their value. \code{"auto"} prints only when both cell
dimensions exceed 40px; \code{TRUE} always prints, shrinking the font to
9px when the smaller dimension is 24–40px (below 24px nothing fits at
any honest size, so nothing prints); \code{FALSE} never prints. The exact
value is always in the cell tooltip.}

\item{truncate_labels}{Maximum length of an axis tick label, in
characters, before it is shortened with an ellipsis (default 24).
Narrow charts may shorten row labels further so the label margin
never eats more than 40\% of the width; the tooltip always carries the
full x and y names.}

\item{xlab, ylab}{Optional axis titles. The heatmap draws none by
default (\code{NULL}) — its axes are self-evident category lists — but a
string here adds one; \code{NA} or \code{""} is the same as \code{NULL}.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
A matrix of two categorical axes with each cell coloured by a numeric
value — the chart for spotting patterns across a whole grid at once.
\code{palette = "sequential"} maps magnitude onto the theme's single-hue
ramp; \code{"diverging"} is for values that span zero and pins the neutral
midpoint there (the colour domain is made symmetric so the two poles
carry equal weight). Cells print their value when there is room,
hovering rings a cell and shows the exact number, and a compact
colour-scale legend sits in the header.
}
\examples{
pop24 <- subset(pv_city_population, year == 2024)
top12 <- head(pop24[order(-pop24$population), "city"], 12)
emp <- subset(pv_city_sectors, city \%in\% top12)
pv_heatmap(emp, x = "city", y = "sector", value = "share",
           title = "Where Swiss city jobs are",
           source = "Source: Bundesamt für Statistik")
}
