% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{pv_report}
\alias{pv_report}
\title{One-call HTML report of a data frame}
\usage{
pv_report(
  data,
  file,
  title = deparse(substitute(data)),
  mode = "auto",
  open = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{file}{Path of the HTML file to write. The widget assets (d3, the
renderers, the bundled Inter font) are copied into a \verb{lib/} folder
next to the file, so move or publish the two together.}

\item{title}{Report heading. Defaults to the expression \code{data} was
called with, so \code{pv_report(airquality, f)} is titled "airquality".}

\item{mode}{\code{"auto"} (default: the page and every widget follow the
viewer's light/dark setting), \code{"light"}, or \code{"dark"}.}

\item{open}{Also open the finished report in the default browser?}
}
\value{
The \code{file} path, invisibly.
}
\description{
Profiles a data frame and writes a standalone, themed HTML report built
from real polyviz widgets — every chart on the page is the same live d3
graphic the individual chart functions produce. The report has a header
with the dimensions and completeness, the \code{\link[=pv_summary]{pv_summary()}} column table
as styled HTML, a \code{\link[=pv_histogram]{pv_histogram()}} for each numeric column (first 8,
two per row), a horizontal \code{\link[=pv_bar]{pv_bar()}} of the most common levels of each
categorical column with at most 30 distinct values (first 4), and —
when the frame has three or more usable numeric columns — a
\code{\link[=pv_heatmap]{pv_heatmap()}} of their pairwise Pearson correlations on the diverging
palette centred at zero. Sections with nothing to show are skipped.
}
\examples{
report <- file.path(tempdir(), "sales-report.html")
pv_report(pv_sales, report, title = "Simulated monthly sales")
}
