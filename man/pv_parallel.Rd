% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-flow.R
\name{pv_parallel}
\alias{pv_parallel}
\title{Interactive D3 parallel coordinates}
\usage{
pv_parallel(
  data,
  columns,
  color = NULL,
  label = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{columns}{Character vector of 2–8 numeric column names — one
vertical axis each, in this order.}

\item{color}{Optional name of a categorical column (max 3 distinct
values keeps overlapping lines distinguishable; more will error).}

\item{label}{Optional name of a column shown first in tooltips.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Each row of the data becomes one line threading across a set of
vertical axes, one per numeric column, each on its own independent
scale. The shape of a line is the row's profile; bundles of similar
shapes are the clusters.
}
\details{
The axes are interactive filters: drag along any axis to brush a value
range — rows outside it fade out, and brushes on several axes combine
so only rows passing all of them stay lit. Double-click an axis to
clear its brush. Hovering a line raises it and reads out the full row.

Rows with a missing value on any axis are dropped — a broken polyline
would be unreadable.
}
\examples{
# Land-use mix per city: share of each city's total area, in percent.
lu <- pv_city_landuse
lu$share <- 100 * lu$hectares / ave(lu$hectares, lu$city, FUN = sum)
wide <- reshape(
  lu[lu$category \%in\% c("Buildings", "Agriculture", "Forest"),
     c("city", "category", "share")],
  direction = "wide", idvar = "city", timevar = "category")
names(wide) <- sub("^share\\\\.", "", names(wide))
pv_parallel(wide, columns = c("Buildings", "Agriculture", "Forest"),
            label = "city", title = "How Swiss cities use their land")
}
