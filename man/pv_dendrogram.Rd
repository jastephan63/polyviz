% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-hierarchy.R
\name{pv_dendrogram}
\alias{pv_dendrogram}
\title{Interactive D3 dendrogram}
\usage{
pv_dendrogram(
  hc,
  labels = NULL,
  k = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{hc}{An object of class \code{hclust}, e.g. from \code{\link[stats:hclust]{stats::hclust()}}.}

\item{labels}{Optional character vector naming the leaves, one entry
per observation clustered. Defaults to \code{hc$labels}, and falls back
to the observation index when the tree carries no labels.}

\item{k}{Optional number of clusters (an integer from 2 to 8 — the
palette carries 8 colours) to colour the tree by, via
\code{\link[stats:cutree]{stats::cutree()}}. \code{NULL} (default) draws the whole tree in one
neutral ink.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Draws the tree inside an \code{\link[stats:hclust]{stats::hclust()}} object: leaves on the
right, reading normally, and every merge drawn as a right-angle
elbow at its actual merge height on a horizontal scale (axis on
top). Give \code{k} to cut the tree into \code{k} clusters the way
\code{\link[stats:cutree]{stats::cutree()}} does — each cluster takes one palette colour and
the branches above the cut stay neutral, so the cut line is visible
at a glance. Hovering any junction highlights its full subtree and
reads out how many leaves it holds and at what height it merged;
hovering a leaf gives its full name (labels may be truncated to
protect the plot).
}
\examples{
# Which large cities have similar economies? Cluster their sector mix.
big <- c("Luzern", "Zug", "Bern", "Basel", "Lausanne", "Winterthur",
         "Biel/Bienne", "St. Gallen")
mix <- stats::xtabs(share ~ city + sector_code,
                    pv_city_sectors[pv_city_sectors$city \%in\% big, ])
hc <- stats::hclust(stats::dist(scale(mix)), method = "ward.D2")
pv_dendrogram(hc, k = 3, title = "Cities by economic structure")
}
