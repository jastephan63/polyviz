% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{pv_fetch_eurostat}
\alias{pv_fetch_eurostat}
\title{Fetch a dataset from Eurostat}
\usage{
pv_fetch_eurostat(id, filter = NULL, start = NULL, end = NULL, refresh = FALSE)
}
\arguments{
\item{id}{Dataset code as Eurostat's data browser shows it, e.g.
\code{"demo_pjan"} (population on 1 January) or \code{"nama_10_gdp"} (GDP and
main components).}

\item{filter}{A single SDMX data key restricting the download to a
slice of the dataset, e.g. \code{"A.NR.TOTAL.T.LU"} - dimension values in
the dataset's dimension order, separated by dots, where an empty
segment keeps all values of a dimension and \code{+} combines several. The
default \code{NULL} downloads the complete dataset. See Details.}

\item{start, end}{First / last period to download, a single year or
period such as \code{"2015"} (sent as the \code{startPeriod} / \code{endPeriod}
query parameters). The default \code{NULL} places no bound.}

\item{refresh}{Set to TRUE to bypass the cache and download again.}
}
\value{
A data frame; source and licence are attached as attributes and
printed on every fetch.
}
\description{
Downloads a dataset from Eurostat's SDMX dissemination API
(\url{https://ec.europa.eu/eurostat/web/main/data/database}) as an SDMX-CSV
export and parses it into a tidy data frame. Downloads are cached (see
\code{\link[=pv_cache_status]{pv_cache_status()}}).
}
\details{
Eurostat's SDMX-CSV carries codes, not labels: each dimension becomes a
lower-cased code column (\code{freq}, \code{unit}, \code{geo}, ...), the observation is
\code{obs_value}, and the time axis is \code{time_period}. Flag columns such as
\code{obs_flag} stay when any row carries a flag and are dropped when empty.
The dataflow reference, source, and licence travel along as attributes
(\code{pv_dataflow}, \code{pv_source}, \code{pv_licence}, \code{pv_url}).

Many Eurostat tables are enormous when pulled whole, so \code{filter} narrows
the download on the server exactly as in \code{\link[=pv_fetch_bfs]{pv_fetch_bfs()}}: a dot-separated
SDMX data key in the dataset's dimension order, where an empty segment
keeps every value of a dimension and \code{+} combines alternatives. For
\code{"demo_pjan"} the order is \code{freq.unit.age.sex.geo}, so
\code{"A.NR.TOTAL.T.LU"} is Luxembourg's total population. The dimension
order and codes are listed on each dataset's page in Eurostat's data
browser. Unlike \code{\link[=pv_fetch_bfs]{pv_fetch_bfs()}} the named-list form is not available
here - Eurostat's structure endpoint does not serve the SDMX-JSON this
package reads, so the key must be spelled out. \code{start} and \code{end} bound
the time axis via \code{startPeriod}/\code{endPeriod}, as on stats.swiss.

Eurostat publishes under its standard reuse policy, the Creative
Commons Attribution 4.0 licence (CC BY 4.0): free use with source
citation ("Source: Eurostat"). A few datasets integrating third-party
data state additional terms on their dataset page.
}
\examples{
\dontrun{
# Luxembourg's population on 1 January, 2015 onwards - the key is
# freq.unit.age.sex.geo, so the unfiltered dataset would be every
# age/sex/country combination since 1960
lux <- pv_fetch_eurostat("demo_pjan", filter = "A.NR.TOTAL.T.LU",
                         start = "2015")

# GDP at market prices for two countries
gdp <- pv_fetch_eurostat("nama_10_gdp", filter = "A.CP_MEUR.B1GQ.CH+LU")
}
}
\seealso{
\code{\link[=pv_fetch_bfs]{pv_fetch_bfs()}}, \code{\link[=pv_fetch_lustat]{pv_fetch_lustat()}}, \code{\link[=pv_cache_status]{pv_cache_status()}}
}
