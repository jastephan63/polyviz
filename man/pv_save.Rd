% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{pv_save}
\alias{pv_save}
\title{Save a chart as PNG, SVG, PDF, or self-contained HTML}
\usage{
pv_save(
  widget,
  file,
  width = 900,
  height = NULL,
  scale = 2,
  mode = "light",
  delay = 0.5,
  embed_fonts = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{widget}{A polyviz chart, as returned by \code{\link[=pv_bar]{pv_bar()}} and friends.}

\item{file}{Output path; the extension (\code{.png}, \code{.svg}, \code{.pdf}, or
\code{.html}) picks the format.}

\item{width}{Chart width in CSS pixels.}

\item{height}{Chart height in CSS pixels. \code{NULL} (default) uses the
fixed height the widget was built with, if any, and 560 otherwise.}

\item{scale}{Resolution multiplier for \code{.png} output: the saved image
is \code{scale} times \code{width} by \code{scale} times \code{height} device pixels.
Other formats are resolution-independent and ignore it.}

\item{mode}{\code{"light"} (default), \code{"dark"}, or \code{"auto"}, forced onto the
saved chart. In an \code{.html} file \code{"auto"} keeps the light/dark
switching live; for captures it renders as light.}

\item{delay}{Extra seconds to wait after the chart looks settled,
before capturing. Raise it for charts that keep loading things.}

\item{embed_fonts}{Inline the bundled Inter font into \code{.svg} and
\code{.html} output as a base64 data URI, so the file renders with the
right type anywhere. \code{FALSE} keeps the file smaller and falls back
to a locally installed Inter (or the system font); print workflows
that post-process the SVG with Inter installed may prefer that.
\code{.png} and \code{.pdf} always carry their fonts and ignore this.}

\item{quiet}{Skip the one-line message saying what was saved?}
}
\value{
The output path, invisibly.
}
\description{
Writes a polyviz chart to disk in the format named by the file
extension, ready to drop into a paper, a slide deck, or an email. The
pixel and vector formats are captured from a real render: headless
Chrome loads the same d3 pipeline the interactive widget uses, the
entrance animation is switched off and the colour \code{mode} is pinned, and
the settled chart is saved once nothing is still drawing — a capture
can never catch a mid-animation frame or inherit the machine's dark
mode. JavaScript errors raised while the page renders are reported as
an R warning quoting the error.
}
\details{
The formats:
\itemize{
\item \code{.png} — raster capture at \code{scale} times the pixel size (the default
2 is a crisp "retina" image).
\item \code{.svg} — one standalone SVG document with the title, legend, plot,
and source line all included as vector shapes and text.
\item \code{.pdf} — a true vector PDF from Chrome's print engine, sized exactly
to the chart; ideal for LaTeX.
\item \code{.html} — the interactive widget as one self-contained file (every
script, stylesheet, and font inlined). This format needs neither
Chrome nor pandoc, and it keeps the entrance animation.
}
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
agg <- aggregate(revenue ~ region, pv_sales, sum)
w <- pv_bar(agg, "region", "revenue", title = "Revenue by region")
pv_save(w, file.path(tempdir(), "revenue.png"))
pv_save(w, file.path(tempdir(), "revenue.svg"))
pv_save(w, file.path(tempdir(), "revenue.pdf"))
pv_save(w, file.path(tempdir(), "revenue.html"))
\dontshow{\}) # examplesIf}
}
