% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstalk.R
\name{pv_link}
\alias{pv_link}
\title{Link a chart into a crosstalk selection group}
\usage{
pv_link(w, sd)
}
\arguments{
\item{w}{A polyviz chart.}

\item{sd}{A \link[crosstalk:SharedData]{crosstalk::SharedData} object wrapping a data frame with
exactly as many rows as the data the chart was built from.}
}
\value{
The chart, carrying the selection keys, the group name, and
the crosstalk JavaScript dependencies - ready for more pipe steps.
}
\description{
Connects a polyviz chart to a \link[crosstalk:SharedData]{crosstalk::SharedData} object so that
selections made in one linked widget - another polyviz chart, a
\code{DT::datatable()}, a \code{crosstalk::filter_select()} - highlight the
matching marks here, and vice versa. The link works in static HTML
(R Markdown, Quarto, \code{htmltools::browsable()}), no Shiny required.
}
\details{
The correspondence is by row: mark \emph{i} of the chart is matched with
row \emph{i} of the shared data, so \code{sd} must wrap the very table (or one
row-aligned with it) that built the chart. Charts that respond to a
linked selection by dimming unselected marks: scatter, beeswarm, bar,
choropleth, and force. Other charts whose payload keeps one row per
mark (line, area, lollipop, and friends) carry the link without
complaint and simply ignore it; charts that aggregate their rows away
(treemap, sunburst, chord, sankey, ...) have nothing to line the keys
up with, and linking one errors.
}
\examples{
f25 <- subset(pv_fiscal, year == 2025)
sd <- crosstalk::SharedData$new(f25)
pv_scatter(f25, x = "resource_index", y = "equalization_chf",
           label = "municipality") |>
  pv_link(sd)
}
