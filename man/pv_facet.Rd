% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/facets.R
\name{pv_facet}
\alias{pv_facet}
\title{Facet a chart into small multiples}
\usage{
pv_facet(w, by, ncol = NULL, share_y = TRUE, share_x = TRUE)
}
\arguments{
\item{w}{A polyviz widget made by \code{\link[=pv_bar]{pv_bar()}}, \code{\link[=pv_line]{pv_line()}},
\code{\link[=pv_scatter]{pv_scatter()}}, or \code{\link[=pv_area]{pv_area()}}.}

\item{by}{A vector with one entry per row of the chart's data,
assigning each row to a panel. Between 2 and 16 distinct levels.}

\item{ncol}{Number of grid columns, or \code{NULL} (default) for a
near-square grid. The renderer never uses more than 4 columns — 2
when the container is narrower than 700 pixels, and a single
scrolling column below 440 — so every panel keeps room for its own
axes and tick labels.}

\item{share_y, share_x}{Share the y (x) axis range across all panels?
Default \code{TRUE}.}
}
\value{
The widget, rewritten into a faceted one.
}
\description{
Splits a bar, line, scatter, or area chart into a grid of panels, one
per level of a grouping vector — the small-multiples layout. Panels
appear in the order their group first appears in \code{by}, each carries
its name at the top left, and the title, subtitle, and source line
are drawn once for the whole grid.
}
\details{
The chart functions keep only the mapped columns, so \code{by} is passed as
the original column of your data frame, not a column name:
\code{pv_scatter(df, x = "wt", y = "mpg") |> pv_facet(df$region)}. It must
line up row-for-row with the data the chart function received. Line
and area charts group their rows by series internally (and sorted
bars re-order by value), so hand those charts data that is already
sorted by series — then the row order survives unchanged and \code{by}
stays aligned.

Bar and line charts refuse data with more than one row per category
or x position. When your panels repeat those keys — the same cities
in every year, say — map the panel variable with \code{series} as well,
exactly as the example below does; each row is then unique and every
panel keeps a single series.

By default all panels share both axis ranges, which is what makes
small multiples comparable: the global x and y domains are computed
here across every panel and injected into the payload as \code{xlim}/
\code{ylim} for the renderers to honour. Set \code{share_y = FALSE} (or
\code{share_x = FALSE}) to let each panel scale to its own data — useful
when the panels live on very different orders of magnitude, but then
the panels no longer compare directly. For stacked areas and stacked
bars the shared y range covers the tallest stack; percent and stream
areas and percent-stacked bars normalise themselves, so \code{share_y} has
nothing to do there.
}
\examples{
pop <- subset(pv_city_population,
              city \%in\% c("Luzern", "Emmen", "Kriens", "Zug"))
pv_line(pop, x = "year", y = "population", series = "city",
        title = "Four cities, four panels") |>
  pv_facet(pop$city, ncol = 2)

fiscal <- subset(pv_fiscal, year >= 2023)
pv_scatter(fiscal, x = "resource_index", y = "equalization_chf") |>
  pv_facet(fiscal$year)
}
