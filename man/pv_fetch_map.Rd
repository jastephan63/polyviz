% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch-map.R
\name{pv_fetch_map}
\alias{pv_fetch_map}
\title{Fetch a Switzerland-wide boundary layer from the BFS}
\usage{
pv_fetch_map(layer = "municipalities", refresh = FALSE)
}
\arguments{
\item{layer}{The layer to fetch. Currently \code{"municipalities"} — the
canton, district, and lake layers are bundled as \link{pv_swiss_cantons},
\link{pv_swiss_districts}, and \link{pv_swiss_lakes} and need no fetching.}

\item{refresh}{Set to TRUE to bypass the cache, download the asset
again, and rebuild the processed layer.}
}
\value{
A GeoJSON FeatureCollection stored as a plain R list, ready
for the \code{map} argument of \code{\link[=pv_choropleth]{pv_choropleth()}} and \code{\link[=pv_bubble_map]{pv_bubble_map()}};
source and licence are attached as attributes and printed on every
fetch.
}
\description{
Downloads the map layers too large to bundle with the package.
\code{pv_fetch_map("municipalities")} returns the boundary polygons of all
Swiss municipalities (status 1 January 2025) as a GeoJSON
FeatureCollection stored as a plain R list — the same layout as
\link{pv_swiss_cantons} — with each feature carrying \code{id} (the official BFS
municipality number) and \code{name}. \code{\link[=pv_choropleth]{pv_choropleth()}} and
\code{\link[=pv_bubble_map]{pv_bubble_map()}} call this for you when given
\code{map = "municipalities"}.
}
\details{
The first call downloads the BFS "Generalisierte Gemeindegrenzen"
(GG25) asset and needs the sf package to reproject it from the Swiss
LV95 grid to the WGS84 longitude/latitude D3 expects; the processed
layer is then kept in the polyviz download cache (see
\code{\link[=pv_cache_status]{pv_cache_status()}}), so later calls are instant, work offline, and no
longer need sf.

The Bundesamt für Statistik publishes these boundaries for open use
with source citation required ("© BFS, ThemaKart"); every fetch prints
these terms and attaches them to the result (\code{pv_source},
\code{pv_licence}, \code{pv_url}).
}
\examples{
\dontrun{
muni <- pv_fetch_map("municipalities")
length(muni$features)
}
}
\seealso{
\link{pv_swiss_cantons}, \code{\link[=pv_cache_status]{pv_cache_status()}}
}
