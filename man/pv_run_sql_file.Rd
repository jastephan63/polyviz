% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql.R
\name{pv_run_sql_file}
\alias{pv_run_sql_file}
\title{Execute a SQL script file}
\usage{
pv_run_sql_file(con, path)
}
\arguments{
\item{con}{A connection from \code{\link[=pv_db_connect]{pv_db_connect()}}.}

\item{path}{Path to a \code{.sql} file.}
}
\value{
The number of statements executed, invisibly.
}
\description{
Runs every statement in a \code{.sql} file against a connection. Statements
are separated by semicolons; \verb{--} line comments are stripped. Useful for
schema definitions and seed data kept as plain SQL alongside R code.
}
\examples{
con <- pv_db_connect()
script <- system.file("sql", "demo.sql", package = "polyviz")
pv_run_sql_file(con, script)
pv_db_tables(con)
pv_db_disconnect(con)
}
