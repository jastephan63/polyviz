% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/locale.R
\name{pv_locale}
\alias{pv_locale}
\title{Set a session-wide chart locale}
\usage{
pv_locale(locale = NULL)
}
\arguments{
\item{locale}{One of \code{"de-CH"}, \code{"fr-CH"}, \code{"it-CH"}, \code{"en-CH"}, or
\code{NULL} (the default) to reset to d3's stock US-English formatting.}
}
\value{
The locale definition now in effect (a list with \code{tag},
\code{number}, and \code{time} entries), or \code{NULL} after a reset, invisibly.
}
\description{
Switches every chart built afterwards in this session to
locale-aware number and date formatting. The packaged locales cover
Switzerland's language regions: \code{"de-CH"}, \code{"it-CH"}, and \code{"en-CH"}
group thousands with the Swiss apostrophe and keep the decimal point
(\verb{10'000.5}), while \code{"fr-CH"} groups with a non-breaking space and
writes the decimal comma (\verb{10 000,5}); each carries the month and
weekday names of its language, so date axes, calendar tooltips, and
race timestamps read \code{février} or \code{März} instead of
\code{February}.
}
\details{
What changes on a chart: tooltips take the locale's grouping and
decimal marks (compact value labels its decimal mark), time axes and
date read-outs take its month and weekday names, and axis ticks of
\code{10000} and up print
grouped in full (\verb{10'000} — the Swiss print convention) up to a
million, above which the compact form stays but wears the locale's
decimal mark (\verb{1,5M} in \code{fr-CH}). Ticks below 10'000 stay ungrouped,
exactly as without a locale, so year axes keep reading \code{2020}.

With no locale set (the default, and after \code{pv_locale(NULL)}) every
chart renders byte-identically to previous polyviz versions: d3's
stock US-English output.

The setting travels inside each widget's payload, so a chart built
while a locale is active keeps it wherever it goes - a saved
\code{.html} file, \code{\link[=pv_save]{pv_save()}} captures, a Shiny app - and charts built
with different locales coexist on one page.
}
\examples{
pv_locale("de-CH")
sales <- aggregate(revenue ~ region, pv_sales, sum)
pv_bar(sales, x = "region", y = "revenue",
       title = "Umsatz nach Region")  # ticks like 10'000
pv_locale(NULL)
}
