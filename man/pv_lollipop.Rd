% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-composition.R
\name{pv_lollipop}
\alias{pv_lollipop}
\title{Interactive D3 lollipop chart}
\usage{
pv_lollipop(
  data,
  x,
  y,
  sort = TRUE,
  value_labels = "auto",
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with at most 40 rows (one per category).
Rows with a missing category or value are dropped with a warning.}

\item{x}{Name of the category column.}

\item{y}{Name of the numeric value column.}

\item{sort}{Sort categories by value, largest first?}

\item{value_labels}{Print the compact value at each head? \code{TRUE}
always, \code{FALSE} never, and \code{"auto"} (the default) shows them only
while the plot area stays at least 200px wide — on narrower charts
they cannot fit, so they are dropped and the tooltip alone carries
the exact figures.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
A ranking chart — the bar chart's lighter cousin: one hairline stem
and dot per category, drawn horizontally so category names stay
upright and readable, with the value at each head. Sorted
largest-first by default. Capped at 40 categories, beyond which no
ranking stays readable.
}
\examples{
f25 <- pv_fiscal[pv_fiscal$year == 2025, ]
top <- head(f25[order(-f25$equalization_chf), ], 15)
pv_lollipop(top, x = "municipality", y = "equalization_chf",
            title = "Largest equalization payments, 2025")
}
