% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-comparison.R
\name{pv_bullet}
\alias{pv_bullet}
\title{Interactive D3 bullet chart}
\usage{
pv_bullet(
  data,
  label,
  value,
  target,
  bands = NULL,
  shared = TRUE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with one row per measure — more than one is
an error; aggregate it first. At most 15 rows (a bullet list is a
compact form). Rows with a missing label, value, or target are
dropped with a warning.}

\item{label}{Name of the column naming each measure.}

\item{value}{Name of the numeric column drawn as the bar. Values
must be non-negative — the bar grows from zero.}

\item{target}{Name of the numeric column drawn as the target tick.
Must be non-negative.}

\item{bands}{Qualitative range thresholds behind the bar, up to 3.
A numeric vector shares the same thresholds across every row; a
character vector of column names reads them per row. Thresholds
must be non-negative and are sorted ascending; each band shades
the ground from zero up to its threshold, darkest first.
\code{NULL} (default) draws no bands.}

\item{shared}{Share one value scale across all rows? \code{TRUE}
(default) draws a single axis under the stack — the honest choice
when the measures share a unit; \code{FALSE} gives every row its own
scale and its own small axis.}

\item{xlab}{Optional value-axis title under the chart. \code{NULL}
(default) draws none; any string draws it.}

\item{ylab}{Ignored (kept for the family signature): the measure
names down the left are their own axis title.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Stephen Few's bullet graph: one compact row per measure, a thin
accent-coloured bar for the value over muted grey qualitative bands,
and a near-black tick marking the target. Rows stack tightly and by
default share one scale, so the eye can run straight down the
column; per-row scales put every measure on its own footing instead.
The greys come from the theme's ink ramp — darkest for the lowest
band, per Few — so the qualitative context never competes with the
value for attention. \code{\link[=pv_textures]{pv_textures()}} leaves this chart alone on
purpose: its bands are already colour-free.
}
\examples{
rev <- aggregate(revenue ~ region, pv_sales, sum)
rev$target <- round(1.08 * mean(rev$revenue), -4)
pv_bullet(rev, label = "region", value = "revenue", target = "target",
          bands = round(max(rev$revenue) * c(0.5, 0.8, 1.1), -4),
          title = "Revenue against target")
}
