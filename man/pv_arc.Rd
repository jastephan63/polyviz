% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-relational.R
\name{pv_arc}
\alias{pv_arc}
\title{Interactive D3 arc diagram}
\usage{
pv_arc(
  nodes,
  links,
  id = "id",
  label = id,
  group = NULL,
  order = "auto",
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{nodes}{Data frame of nodes, one row each.}

\item{links}{Data frame of links with \code{source}/\code{target} columns
holding node ids, and optionally \code{value} for flow size (arc width;
non-negative, no missing values; 1 when the column is absent).
Repeating the same source/target pair is an error - aggregate those
rows first. A link from a node to itself has no arc to draw and is
refused too. Opposite directions (\code{A} to \code{B} and \code{B} to \code{A}) are
allowed but draw on the same curve, one arc over the other; sum the
directions first if one arc per pair reads better.}

\item{id}{Name of the node id column (default \code{"id"}). Ids must be
unique - each node is one dot on the line.}

\item{label}{Name of the node label column (defaults to the id
column).}

\item{group}{Optional name of a grouping column mapped to colour.
Colours are assigned to group levels in first-appearance order over
the \code{nodes} you passed, so they stay put whichever \code{order} is
chosen. At most as many levels as the active theme's palette has
colours (8 in the packaged theme).}

\item{order}{How to arrange the nodes along the line. \code{"auto"}
(default) applies the crossing-reduction heuristic described above;
\code{"none"} keeps the \code{nodes} arrival order; a character vector of
node ids (a permutation of them - anything else is an error) places
the nodes exactly in that order, left to right.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
A network laid out for reading: every node sits on one horizontal
line, labelled plainly below it, and each link bows over the line as
an arc. Where the force layout (\code{\link[=pv_force]{pv_force()}}) shows a network's shape,
the arc diagram shows its members - the line keeps every label
horizontal and legible, which makes it the right network view when
the names matter as much as the wiring.
}
\details{
Dots are sized gently by their number of connections (on a square-root
scale), arcs are as wide as the square root of their \code{value}, and arc
colour follows the source node's \code{group}. Hovering a node lights up
its arcs and fades the rest; hovering an arc reads out source, target,
and value. When labels get tight they stagger into two rows and then
shorten with an ellipsis - the tooltip always carries the full name.

Node order is the whole layout, so \code{order} controls it explicitly.
\code{"auto"} (the default) runs a small crossing-reduction heuristic:
repeated barycenter sweeps (each node pulled toward the average
position of its neighbours), polished by greedy swaps of neighbouring
nodes wherever a swap removes crossings. Neither stage can end worse
than it started, so \code{"auto"} never draws more arc crossings than the
arrival order; on the bundled \link{pv_network} collaboration graph it
removes about a quarter of them. Hub-and-spoke graphs, where every
link touches one centre, have no crossings under any order - there
\code{"auto"} simply pulls each hub next to its spokes.

In Shiny, clicking a node reports
\code{list(part = "node", id, label, group, connections)} and clicking an
arc \code{list(part = "link", source, target, value)} as
\verb{input$<outputId>_click}.
}
\examples{
pv_arc(pv_network$nodes, pv_network$links, group = "group",
       title = "The collaboration network, name by name")
# The same graph in arrival order, for comparison:
pv_arc(pv_network$nodes, pv_network$links, group = "group",
       order = "none")
}
