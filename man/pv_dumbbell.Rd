% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-comparison.R
\name{pv_dumbbell}
\alias{pv_dumbbell}
\title{Interactive D3 dumbbell chart}
\usage{
pv_dumbbell(
  data,
  y,
  x1,
  x2,
  labels = NULL,
  sort = "gap",
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with one row per category — more than one
is an error; aggregate it first. At most 40 rows (beyond that no
ranking stays readable). Rows with a missing category or value are
dropped with a warning.}

\item{y}{Name of the category column (one row of the chart per level).}

\item{x1, x2}{Names of the two numeric value columns — the dot pair
each connector joins.}

\item{labels}{Names for the two value dots, in \code{x1}, \code{x2} order —
e.g. \code{c("2010", "2024")}. They feed the legend and the tooltip.
\code{NULL} (default) uses the two column names.}

\item{sort}{Row order, top to bottom. \code{"gap"} (default) sorts by
\code{x2 - x1}, largest gain first; \code{"x1"} and \code{"x2"} sort by that end,
largest first; \code{FALSE} keeps the rows as they arrive.}

\item{xlab}{Optional value-axis title under the chart. \code{NULL}
(default) draws none — with the legend naming both dot sets a
title is usually redundant; any string draws it.}

\item{ylab}{Ignored (kept for the family signature): the category
names down the left are their own axis title.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Two values per category joined by a connector — before and after,
here and there — with the categories running down the left like a
horizontal bar chart, so long names stay upright and readable. The
first value is a quiet grey dot, the second wears the accent colour,
and a two-entry legend above the chart names them. Where the
connector is long enough to carry it, the gap between the two values
is written right at its midpoint. Hovering a row shows both exact
values and the change, absolute and in percent.
}
\examples{
f20 <- pv_fiscal[pv_fiscal$year == 2020,
                 c("municipality", "resource_index")]
f27 <- pv_fiscal[pv_fiscal$year == 2027,
                 c("municipality", "resource_index")]
both <- merge(f20, f27, by = "municipality",
              suffixes = c("_2020", "_2027"))
top <- head(both[order(-both$resource_index_2027), ], 12)
pv_dumbbell(top, y = "municipality", x1 = "resource_index_2020",
            x2 = "resource_index_2027", labels = c("2020", "2027"),
            title = "Tax strength, first vs latest year")
}
