% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme-api.R
\name{pv_check_palette}
\alias{pv_check_palette}
\alias{print.pv_palette_check}
\title{Check a categorical palette for accessibility}
\usage{
pv_check_palette(colors, mode = c("light", "dark"), surface = NULL)

\method{print}{pv_palette_check}(x, ...)
}
\arguments{
\item{colors}{Character vector of colours (hex strings or R colour
names), in the order series will receive them.}

\item{mode}{\code{"light"} or \code{"dark"} — the surface the palette will
render on, which picks the lightness band and default surface.}

\item{surface}{Optional surface colour to test contrast against.
Defaults to the packaged chart surface for \code{mode} (\code{"#fbf9f5"}
light, \code{"#1b1a18"} dark).}

\item{x, ...}{A \code{pv_palette_check} object and further arguments
(ignored), for the print method.}
}
\value{
An object of class \code{"pv_palette_check"}: a list with \code{colors},
\code{mode}, \code{surface}, \code{ok} (\code{TRUE} when no check failed hard), \code{checks}
(per-check \code{status} — \code{"pass"}, \code{"warn"}, \code{"fail"}, or \code{"note"} —
plus the offending colours or pairs), \code{suggested_order} (integer
permutation of the input), and the worst adjacent CVD Delta E before
(\code{input_worst}) and after (\code{suggested_worst}) reordering. Printing
it gives a readable report.
}
\description{
Runs the accessibility gauntlet the packaged polyviz palette was built
against, in OKLab/OKLCH space:
}
\details{
\itemize{
\item \strong{lightness} — every colour's OKLab L must sit inside the band for
its surface (\code{0.43}–\code{0.77} on light, \code{0.48}–\code{0.67} on dark), so
nothing reads washed-out or inky.
\item \strong{chroma} — OKLCH chroma of at least \code{0.10}, so no colour collapses
into grey.
\item \strong{cvd} — adjacent pairs must stay apart for colour-deficient
viewers: the smaller of the protanopia/deuteranopia distances
(Machado et al. 2009, severity 1, simulated in linear RGB; Delta E =
100 × OKLab distance) must reach \code{8}. Pairs at \code{6}–\code{8} pass with a
warning — usable only when a secondary encoding (direct labels,
position, pattern) carries the series identity too.
\item \strong{separation} — adjacent pairs need normal-vision Delta E of at
least \code{15}.
\item \strong{contrast} — colours should reach WCAG contrast \code{3:1} against the
chart surface; ones that fall short get a note (give their marks a
thin surface-coloured outline), not a failure.
}

Only \emph{adjacent} pairs are gated because polyviz assigns colours in slot
order — the order is part of the accessibility contract. The result
therefore also carries \code{suggested_order}: the ordering of your input
(exhaustive search up to 8 colours) that maximises the worst adjacent
pair under colour-vision deficiency, keeping your first colour first
when possible.
}
\examples{
# The packaged palette passes its own gauntlet
pv_check_palette(pv_palette(8))

# Two near-identical greens side by side do not
chk <- pv_check_palette(c("#006ba2", "#4f9d3f", "#529f42", "#db444b"))
chk$ok
chk$suggested_order
}
