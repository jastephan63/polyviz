% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opendata.R
\name{pv_fetch_opendata}
\alias{pv_fetch_opendata}
\title{Fetch a dataset's CSV data from opendata.swiss}
\usage{
pv_fetch_opendata(dataset, resource = NULL, refresh = FALSE)
}
\arguments{
\item{dataset}{Dataset slug or id on opendata.swiss, e.g.
\code{"finanzausgleich-kanton-luzern"} - the \code{id} column of a
\code{\link[=pv_search_opendata]{pv_search_opendata()}} result.}

\item{resource}{Which resource to download when the dataset has several:
\code{NULL} (the default) takes the dataset's first CSV resource, a number
\code{n} its \code{n}-th CSV resource, and a string is matched against resource
ids, URLs, and file names.}

\item{refresh}{Set to TRUE to bypass the cache and download again.}
}
\value{
A data frame; source and licence are attached as attributes and
printed on every fetch.
}
\description{
Looks a dataset up on opendata.swiss, downloads one of its CSV
resources from the publishing organisation's server, and parses it into
a data frame. Downloads are cached (see \code{\link[=pv_cache_status]{pv_cache_status()}}); the source
and licence terms are printed on every fetch and attached to the result
(\code{pv_source}, \code{pv_licence}, \code{pv_url}).
}
\details{
Only openly licensed resources are delivered. A resource whose licence
is not one of the opendata.swiss open-data terms (or another open
licence) is refused with a message - including datasets that state no
licence at all. Note that "OPEN ASK"/"OPEN BY ASK" resources are open
data whose commercial use requires the data owner's permission; the
printed terms say so.
}
\examples{
\dontrun{
fiscal <- pv_fetch_opendata("finanzausgleich-kanton-luzern",
                            resource = "fa-lu-ra.csv")
str(fiscal)
}
}
\seealso{
\code{\link[=pv_search_opendata]{pv_search_opendata()}}, \code{\link[=pv_cache_status]{pv_cache_status()}}
}
