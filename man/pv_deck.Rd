% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deck.R
\name{pv_deck}
\alias{pv_deck}
\title{Save a list of charts as a PowerPoint deck}
\usage{
pv_deck(
  charts,
  file,
  title = NULL,
  subtitle = NULL,
  width = 10,
  height = 5.63,
  scale = 2,
  mode = "light",
  ratio = NULL,
  quiet = FALSE
)
}
\arguments{
\item{charts}{A list of polyviz charts, one per slide, in slide order.
A single chart may be given bare.}

\item{file}{Output path, ending in \code{.pptx}.}

\item{title}{Optional deck title; when given, the deck opens with a
title slide.}

\item{subtitle}{Optional line under the title on the title slide;
needs \code{title}.}

\item{width, height}{Slide size in inches. The default is the standard
16:9 slide.}

\item{scale}{Resolution multiplier for the slide renders: each slide
PNG is \code{scale} times its pixel size (the default 2 is a crisp
"retina" image, 192 pixels to the inch).}

\item{mode}{\code{"light"} (default), \code{"dark"}, or \code{"auto"}, forced onto
every rendered slide. Slides are static captures, so \code{"auto"}
renders as light, exactly as in \code{\link[=pv_save]{pv_save()}}.}

\item{ratio}{Convenience preset for the slide size: \code{"16:9"} or
\code{"4:3"}. Give either \code{ratio} or explicit \code{width}/\code{height}, not both.}

\item{quiet}{Skip the one-line message saying what was saved?}
}
\value{
The output path, invisibly.
}
\description{
Writes one \code{.pptx} file with one slide per chart. Every slide is a real
render: the chart goes through the same headless-Chrome pipeline as
\code{\link[=pv_save]{pv_save()}} at exactly the slide's dimensions (96 pixels to the inch,
times \code{scale}), and the settled PNG is placed edge to edge on a blank
layout — what lands in the deck is pixel for pixel what a \code{pv_save()}
capture shows. When \code{title} is given the deck opens with a title slide
on the same chart surface, set in Inter like the charts themselves;
on machines without Inter installed PowerPoint silently substitutes a
system font. Each chart's title and source line also go
into its slide's speaker notes, so the deck stays searchable and
presentable even though the slides are images.
}
\details{
Slides are 16:9 by default (10 x 5.63 inches). \code{ratio = "4:3"} switches
to the classic 10 x 7.5, or give explicit \code{width}/\code{height} for anything
else.

Needs the officer package to write the file and chromote (with a
Chrome-based browser) to render the slides; both install with
\code{install.packages()}.
}
\examples{
\dontshow{if (interactive()) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
agg <- aggregate(revenue ~ region, pv_sales, sum)
deck <- list(
  pv_bar(agg, "region", "revenue", title = "Revenue by region"),
  pv_donut(agg, "region", "revenue", title = "Share of revenue")
)
pv_deck(deck, file.path(tempdir(), "review.pptx"),
        title = "Quarterly review")
\dontshow{\}) # examplesIf}
}
