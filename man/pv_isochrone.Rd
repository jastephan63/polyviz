% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-geo.R
\name{pv_isochrone}
\alias{pv_isochrone}
\title{Interactive D3 isochrone map of Switzerland}
\usage{
pv_isochrone(
  data,
  lat,
  lon,
  minutes,
  origin = NULL,
  breaks = c(30, 60, 90, 120),
  palette = "sequential",
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with one row per point. Rows with a missing
coordinate are dropped with a warning, as are points falling
outside Switzerland; rows whose travel time is \code{NA} are unreached
and take no part in the interpolation.}

\item{lat, lon}{Names of the numeric coordinate columns, in WGS84
degrees (latitude north, longitude east — \link{pv_city_coords} is the
bundled template).}

\item{minutes}{Name of the numeric column holding each point's travel
time in minutes. Must be non-negative; \code{NA} means unreached.}

\item{origin}{Optional journey start, drawn as a ringed marker: a
numeric length-2 vector, \code{c(lat, lon)} in the order of the \code{lat}
and \code{lon} arguments (or named, \code{c(lat = ..., lon = ...)}). Must
fall on the map. \code{NULL} (default) draws no marker.}

\item{breaks}{The band edges in minutes, strictly increasing and
positive. The default \code{c(30, 60, 90, 120)} yields five bands:
0–30, 31–60, 61–90, 91–120, and over 120 minutes.}

\item{palette}{Only \code{"sequential"}: travel time is a magnitude with
no reference point to diverge around, so the single-hue ramp is
the one honest scale. The argument exists so the isochrone reads
like its geo siblings.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Travel-time bands over the Swiss map — the chart for "how far can I
get" questions. Each row of \code{data} is one point with coordinates and
a travel time in minutes: the station travel times \code{pv_transit_times()}
fetches are the natural input, but any point measure in minutes works
— drive times, delivery times, response times. The country is banded
at the \code{breaks} on the theme's stepped sequential ramp, light where
the origin is near and dark where it is far, with the Swiss lakes
drawn over the bands and the canton borders as hairlines on top.
Hovering a band reads its range ("31–60 min"), and \code{origin} puts a
ringed marker where the journey starts.
}
\details{
The surface between the points is a principled nearest-service
interpolation, not a guess: the country is rasterised to a 160×100
grid, and each cell's time is the minimum over all points of that
point's minutes plus the walk from the cell centre to the point at
5 km/h along the great circle — the fastest "ride there, walk the
rest" combination the points allow. Cells farther than a 30-minute
walk from every point stay empty, so the map never paints service
onto terrain no point serves; those unreached areas keep the plain
chart surface. Points with a missing travel time count as unreached
— exactly how \code{pv_transit_times()} marks stations the network never
gets to — and simply sit out the interpolation.
}
\examples{
# Hand-set demo times from Lucerne - illustrative geometry, not a
# timetable claim.
reach <- data.frame(
  lat = c(47.05, 47.17, 47.38, 46.95, 47.56),
  lon = c(8.31, 8.52, 8.54, 7.45, 7.59),
  minutes = c(0, 21, 41, 62, 61))
pv_isochrone(reach, lat = "lat", lon = "lon", minutes = "minutes",
             origin = c(47.05, 8.31),
             title = "How far Lucerne reaches")
}
