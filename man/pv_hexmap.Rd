% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-geo.R
\name{pv_hexmap}
\alias{pv_hexmap}
\title{Interactive D3 hex cartogram of Switzerland}
\usage{
pv_hexmap(
  data,
  id,
  value,
  palette = c("sequential", "diverging"),
  center = NULL,
  labels = TRUE,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with at most one row per canton.}

\item{id}{Name of the column identifying each canton: two-letter
codes or BFS numbers 1–26 (mixing the two forms is fine). Missing
ids are dropped with a warning; an id that matches no canton is an
error listing the offenders.}

\item{value}{Name of the numeric column mapped to colour. Rows with a
missing value are dropped with a warning — their cantons read as
"no data".}

\item{palette}{\code{"sequential"} (default, for magnitudes) or
\code{"diverging"} (for values around a reference point, which needs
\code{center}).}

\item{center}{The reference value the diverging palette's neutral
midpoint stands for (e.g. \code{100} for an index, \code{0} for a change).
Required — and only allowed — when \code{palette = "diverging"}.}

\item{labels}{Draw each canton's two-letter code in its hexagon?
\code{TRUE} (default) or \code{FALSE}.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
The 26 cantons as equal-sized hexagons, coloured by a numeric value —
the map for when every canton's number matters equally. On a real
choropleth the geography does the weighting: Graubünden and Valais
dominate the picture while the dense little cantons — Basel-Stadt,
Zug, Geneva — all but vanish, exactly the places where much of the
story usually lives. The hexmap trades faithful shapes for equal
visual weight: one hexagon per canton, arranged on a hand-curated
grid that keeps the country's neighbourhoods (Basel-Stadt in the
northwest corner, Schaffhausen on the northern rim, Ticino south of
the Gotthard, Geneva at the far southwestern tip), so the map still
reads as Switzerland at a glance.
}
\details{
\code{data} joins onto the grid through \code{id}, which may hold either the
two-letter canton codes (\code{"ZH"}, \code{"BE"}, ..., \code{"JU"}, matched
case-insensitively) or the BFS canton numbers 1–26 (the same numbers
\link{pv_swiss_cantons} joins on, as numbers or digit strings) — the two
forms can even mix. An id matching no canton is an error, not a
silent gap: an unmatched canton would simply vanish from a map whose
whole point is that no canton vanishes.

\code{palette} and \code{center} work exactly as in \code{\link[=pv_choropleth]{pv_choropleth()}}:
\code{"sequential"} maps magnitude onto the theme's single-hue ramp,
\code{"diverging"} pins the neutral midpoint colour to the reference value
in \code{center} and makes the colour domain symmetric around it. Cantons
without a data row keep a neutral fill and a dashed outline, so
absence stays visible. Each hexagon wears its canton's two-letter
code (switch that off with \code{labels = FALSE}); hovering shows the full
canton name and the exact value, and the choropleth's colour-scale
legend sits in the header. The hexagons size themselves to the
container and re-fit on resize.
}
\examples{
nights24 <- aggregate(nights ~ canton_id,
                      subset(pv_tourism, year == 2024), sum)
pv_hexmap(nights24, id = "canton_id", value = "nights",
          title = "Where Switzerland's guests sleep")
# Two-letter codes join too, and a diverging palette takes a center:
idx <- data.frame(canton = c("ZH", "BE", "LU", "BS", "GE"),
                  index = c(112, 95, 101, 124, 118))
pv_hexmap(idx, id = "canton", value = "index",
          palette = "diverging", center = 100)
}
