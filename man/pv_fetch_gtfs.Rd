% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtfs.R
\name{pv_fetch_gtfs}
\alias{pv_fetch_gtfs}
\title{Fetch the Swiss national public-transport timetable (GTFS)}
\usage{
pv_fetch_gtfs(refresh = FALSE)
}
\arguments{
\item{refresh}{Set to TRUE to re-resolve the newest weekly export,
download it again, and rebuild the extracted copy.}
}
\value{
A handle of class \code{pv_gtfs}: a list carrying the extract
directory (\code{dir}), the open DuckDB connection (\code{con}, one view per
GTFS file), the file names found (\code{files}), and the feed's
\code{feed_info} row. Printing it shows the feed's validity window and a
few counts.
}
\description{
Downloads the newest weekly GTFS export of the complete Swiss timetable
("Fahrplan 2026") from opentransportdata.swiss, extracts it into the
polyviz download cache, and opens it as a live DuckDB session with one
view per GTFS file (\code{stops}, \code{trips}, \code{stop_times}, \code{calendar}, ...).
The returned handle is what \code{\link[=pv_transit_times]{pv_transit_times()}} runs on; close it with
\code{\link[=pv_gtfs_close]{pv_gtfs_close()}} when done.
}
\details{
Be warned about the size before the first call: the ZIP is about
240 MB - by far the largest download in this package - and unpacks to
roughly 4 GB of text (35 million \code{stop_times} rows alone). Both the ZIP
and the extracted files stay in the cache
(\code{tools::R_user_dir("polyviz", "cache")}), so later calls open the
feed in a couple of seconds without any network. Nothing is loaded
into R: DuckDB reads the extracted files in place, which is why the
handle needs the duckdb package (a Suggests dependency).

Which weekly export counts as "newest" is resolved through a catalogue
lookup that is itself cached, so an installed feed stays put between
sessions; \code{refresh = TRUE} re-resolves the newest week, downloads it,
and re-extracts.

The feed is published by SBB on behalf of the Federal Office of
Transport under the opentransportdata.swiss open-data terms ("ODMCH"):
free use, source citation required ("Quelle:
opentransportdata.swiss"). Every fetch prints these terms and attaches
them to the handle (\code{pv_source}, \code{pv_licence}, \code{pv_url}).
}
\examples{
\dontrun{
gtfs <- pv_fetch_gtfs() # first call: ~240 MB download + extraction
gtfs
times <- pv_transit_times(gtfs, "Luzern", "2026-09-15")
pv_gtfs_close(gtfs)
}
}
\seealso{
\code{\link[=pv_transit_times]{pv_transit_times()}}, \code{\link[=pv_gtfs_close]{pv_gtfs_close()}}, \code{\link[=pv_cache_status]{pv_cache_status()}}
}
