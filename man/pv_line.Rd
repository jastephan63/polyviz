% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widgets.R
\name{pv_line}
\alias{pv_line}
\title{Interactive D3 line chart}
\usage{
pv_line(
  data,
  x,
  y,
  series = NULL,
  legend = "auto",
  show_points = FALSE,
  curve = c("linear", "monotone", "step"),
  zoom = FALSE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 800,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with one row per x position per series —
more than one is an error; aggregate first. Rows with a missing x
or value are dropped with a warning.}

\item{x}{Name of the x column — \code{Date}, numeric, or categorical.}

\item{y}{Name of the numeric value column.}

\item{series}{Optional name of a series column (one line per level).}

\item{legend}{Show the colour legend row above the chart? \code{TRUE} always
shows it, \code{FALSE} hides it (the direct labels at the line ends
remain); \code{"auto"} (default) shows it exactly when a \code{series} mapping
with more than one level exists.}

\item{show_points}{Mark every observation with a dot on its line —
the connected-scatter treatment. \code{TRUE} always, \code{FALSE} (default)
never; \code{"auto"} shows the dots when the individual observations
matter: no series longer than 30 points, with at least about 12
horizontal pixels between neighbouring dots. Each dot takes its
line's colour with the usual 2px surface ring, and the crosshair
tooltip works exactly as before. Spaghetti charts (more series than
the active theme's palette has hues) never draw them — their lines
share one muted ink, so per-point dots would only add noise.}

\item{curve}{How the line travels between observations. \code{"linear"}
(default) connects them with straight segments; \code{"monotone"} draws
a smoothed curve that still passes through every point without
overshooting it; \code{"step"} holds each value flat until the next
observation — the honest shape for rates and thresholds that change
at discrete moments.}

\item{zoom}{Add a brush-to-zoom strip below the chart? \code{FALSE}
(default) or \code{TRUE}; needs a date or numeric x axis. The strip is a
muted miniature of the full series: dragging across it narrows the
main panel to that x window, and double-clicking the strip (or
clicking it outside the brushed window) restores the full range.
The chart always opens at the full range, so any export shows the
complete series; SVG and PDF exports leave the strip out entirely,
while a PNG page capture keeps the muted strip in view.
\code{\link[=pv_facet]{pv_facet()}} drops the strip quietly: its panels share axes, which
a per-panel brush would break.}

\item{xlab, ylab}{Axis titles. \code{NULL} (default) uses the column names;
\code{NA} or \code{""} suppresses the title entirely; any other string
replaces it.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Multi-series line chart with a draw-in animation and a crosshair
tooltip that reads out every series at the hovered x position. Series
are direct-labelled at the line ends. \code{curve} picks how the line
travels between observations, \code{show_points} marks each observation
with a dot, and \code{zoom} adds a brush-to-zoom strip for long series.
}
\examples{
monthly <- aggregate(revenue ~ month + region, pv_sales, sum)
pv_line(monthly, x = "month", y = "revenue", series = "region")
}
