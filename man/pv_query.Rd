% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql.R
\name{pv_query}
\alias{pv_query}
\title{Run a SQL query and return the result}
\usage{
pv_query(con, sql, params = NULL)
}
\arguments{
\item{con}{A connection from \code{\link[=pv_db_connect]{pv_db_connect()}}.}

\item{sql}{A single SQL statement. Use \verb{?} placeholders with \code{params}
for values — never paste user input into the SQL string.}

\item{params}{Optional list of values bound to \verb{?} placeholders.}
}
\value{
A data frame.
}
\description{
Run a SQL query and return the result
}
\examples{
con <- pv_db_connect()
pv_db_write(con, "cars", mtcars)
pv_query(con, "SELECT cyl, AVG(mpg) AS mpg FROM cars GROUP BY cyl")
pv_query(con, "SELECT * FROM cars WHERE cyl = ?", params = list(6))
pv_db_disconnect(con)
}
