% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{pv_read}
\alias{pv_read}
\title{Read data from any supported source}
\usage{
pv_read(path, table = NULL, ...)
}
\arguments{
\item{path}{Path to a data file.}

\item{table}{For database files: the table to read. Defaults to the
only table in the database; an error lists the options when there are
several.}

\item{...}{Passed to the underlying reader (e.g. \code{stringsAsFactors} for
CSV).}
}
\value{
A data frame.
}
\description{
One entry point that dispatches on file extension:
}
\details{
\tabular{ll}{
   Extension \tab Backend \cr
   \code{.csv}, \code{.tsv} \tab base R \cr
   \code{.rds} \tab base R \cr
   \code{.sas7bdat}, \code{.xpt} \tab SAS via \code{haven} \cr
   \code{.sqlite}, \code{.db}, \code{.s3db} \tab SQL via \code{DBI}/\code{RSQLite} \cr
   \code{.duckdb}, \code{.ddb} \tab SQL via \code{DBI}/\code{duckdb} \cr
   \code{.parquet} \tab DuckDB via \code{duckdb} \cr
}


Parquet files are read in place by DuckDB, which needs the \code{duckdb}
package (a Suggests dependency) installed.
}
\examples{
db <- system.file("extdata", "demo.sqlite", package = "polyviz")
sales <- pv_read(db, table = "sales")
head(sales)
}
