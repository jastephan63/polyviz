% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-distribution.R
\name{pv_histogram}
\alias{pv_histogram}
\title{Interactive D3 histogram}
\usage{
pv_histogram(
  data,
  x,
  bins = NULL,
  density = FALSE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Name of the numeric column to bin.}

\item{bins}{Number of equal-width bins, or \code{NULL} for R's Sturges
default.}

\item{density}{Overlay a kernel density curve?}

\item{xlab, ylab}{Axis titles. \code{NULL} (the default) uses the \code{x} column
name for x and \code{"count"} for y; \code{NA} or \code{""} suppresses the title;
any other string replaces it.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
The distribution of one numeric column as bars over equal-width bins,
with a tooltip giving each bin's exact range and count. Binning follows
R's own \code{\link[graphics:hist]{graphics::hist()}} rules (Sturges by default), so the shape
matches what base R would show. With \code{density = TRUE} a kernel density
curve is overlaid, rescaled to count space (density × n × bin width) so
the curve and the bars share one honest y axis.
}
\examples{
lucerne25 <- subset(pv_fiscal, year == 2025)
pv_histogram(lucerne25, x = "resource_per_capita", bins = 20,
             density = TRUE,
             title = "Tax resources per resident, 2025")
}
