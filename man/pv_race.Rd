% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-motion.R
\name{pv_race}
\alias{pv_race}
\title{Animated D3 bar-chart race}
\usage{
pv_race(
  data,
  time,
  id,
  value,
  top_n = 10,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame in long form: one row per entity per time
point (missing combinations mean the entity is absent then).}

\item{time}{Name of the time column — numeric (e.g. a year) or \code{Date}.
At least 2 distinct time points are required.}

\item{id}{Name of the column identifying each racing entity.}

\item{value}{Name of the numeric column the entities race on
(non-negative; \code{NA} counts as absent).}

\item{top_n}{How many ranked bars are visible at once (a whole number,
at least 2; capped at the number of entities that ever qualify).}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Tempo control: each step between consecutive time
points takes \code{duration / 500 * 900} ms (never under 200 ms), so the
default \code{500} gives roughly 900 ms per step. \code{0} skips the
animation entirely and shows
the final time point — no autoplay, but the replay control stays,
and pressing it runs the race at the default tempo.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
The bar-chart race: at every time point the \code{top_n} entities appear as
horizontal bars ranked by value, and the animation interpolates both
values and ranks between consecutive time points, so bars grow and
overtake each other smoothly while a large muted time label ticks
along in the corner. Value labels ride the bar ends. Entities that
rise into the ranking slide in from below the last visible row;
entities that fall out slide down the same way. When the race
finishes, a small circled-arrow control in the top-right corner
replays it. Hovering a bar reads out the entity's exact value at the
moment shown.
}
\details{
The entities in the race are everyone who makes the \code{top_n} at \emph{any}
time point, so late risers and early leaders all get their moment.
Colours follow the final standings in palette-slot order; beyond the
palette's 8 slots the colours repeat, faded toward the background.
}
\examples{
pv_race(pv_city_population, time = "year", id = "city",
        value = "population", top_n = 12,
        title = "Swiss cities racing through a century",
        source = "Source: Bundesamt für Statistik")
}
