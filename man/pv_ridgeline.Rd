% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-distribution.R
\name{pv_ridgeline}
\alias{pv_ridgeline}
\title{Interactive D3 ridgeline chart}
\usage{
pv_ridgeline(
  data,
  value,
  group,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Name of the numeric column whose distribution is drawn.
Rows with a missing value or group are dropped with a warning.}

\item{group}{Name of the grouping column (one ridge per level, max 8;
every level needs at least 2 non-missing values).}

\item{xlab}{X-axis title. \code{NULL} (the default) uses the \code{value} column
name; \code{NA} or \code{""} suppresses it; any other string replaces it.}

\item{ylab}{Optional rotated title beside the group labels. Ridges are
labelled directly, so \code{NULL} (the default) draws none, same as \code{NA}
or \code{""}; a string adds one.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Overlapping kernel density curves, one ridge per group, ordered by
median from the top down — the readable way to compare the shape of a
distribution across up to 8 groups. Hovering a ridge lifts it to full
opacity, dims the rest, and shows the group's median and size.
}
\examples{
pv_ridgeline(pv_fiscal, value = "resource_index", group = "year",
             title = "Resource index across municipalities")
}
