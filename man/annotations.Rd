% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modifiers.R
\name{annotations}
\alias{annotations}
\alias{pv_hline}
\alias{pv_vline}
\alias{pv_band}
\alias{pv_note}
\title{Annotation layers for polyviz charts}
\usage{
pv_hline(at, label = NULL, color = NULL)

pv_vline(at, label = NULL, color = NULL)

pv_band(x0 = NULL, x1 = NULL, y0 = NULL, y1 = NULL, label = NULL)

pv_note(x, y, text, dx = 0, dy = 0)
}
\arguments{
\item{at}{Where the reference line crosses its axis: a y value for
\code{pv_hline()}, an x value for \code{pv_vline()}.}

\item{label}{Optional short text drawn next to the line or inside the
band.}

\item{color}{Optional line/label colour. \code{NULL} (default) uses the
theme's secondary ink, which is the right choice almost always -
reference lines should recede, not compete with the data.}

\item{x0, x1}{Both ends of a vertical band on the x axis.}

\item{y0, y1}{Both ends of a horizontal band on the y axis. Give
exactly one pair - \code{x0}+\code{x1} or \code{y0}+\code{y1}, never both.}

\item{x, y}{Data position the note points at.}

\item{text}{The note's text. Keep it short - it is drawn on the chart,
not in a tooltip.}

\item{dx, dy}{Pixel offset of the text from its anchor point. When
either is non-zero, a thin leader line connects text and anchor.}
}
\value{
An annotation object to pass to \code{\link[=pv_annotate]{pv_annotate()}}.
}
\description{
Small builder functions for the objects \code{\link[=pv_annotate]{pv_annotate()}} accepts:
\code{pv_hline()} and \code{pv_vline()} draw dashed reference lines across the
plot, \code{pv_band()} shades a region behind the data, and \code{pv_note()}
places a short text label at a data position, with an optional leader
line when it is nudged away from its anchor.
}
\details{
Positions are given in data units. \code{Date} values are converted to the
ISO strings the charts use internally, and category values (say a bar
chart's category name) pass through the axis scale unchanged, so
\code{pv_vline("Luzern")} works as well as \code{pv_vline(100)}.
}
\examples{
f25 <- subset(pv_fiscal, year == 2025)
pv_scatter(f25, x = "resource_index", y = "equalization_chf") |>
  pv_annotate(pv_vline(100, label = "cantonal average"),
              pv_band(x0 = 36, x1 = 100))
}
