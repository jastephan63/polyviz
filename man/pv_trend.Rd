% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trends.R
\name{pv_trend}
\alias{pv_trend}
\title{Add a fitted trend to a scatter or line chart}
\usage{
pv_trend(w, method = c("loess", "lm"), level = 0.95, slot = 2, span = 0.75)
}
\arguments{
\item{w}{A polyviz scatter or line chart.}

\item{method}{\code{"loess"} (default) for a local smoother, \code{"lm"} for a
straight least-squares line.}

\item{level}{Confidence level for the ribbon (default \code{0.95}). \code{NA}
drops the ribbon and draws the fitted line alone.}

\item{slot}{Which palette slot colours the trend (default \code{2}, so it
contrasts with data drawn in slot 1).}

\item{span}{The loess smoothing span (default \code{0.75}); larger is
smoother. Ignored by \code{"lm"}.}
}
\value{
The chart, with the fitted trend attached - ready for more
pipe steps.
}
\description{
Fits a smooth curve (\code{"loess"}, the default) or a straight regression
line (\code{"lm"}) through the chart's points and overlays it, with a
shaded confidence ribbon. The model runs in R via \code{\link[stats:loess]{stats::loess()}} or
\code{\link[stats:lm]{stats::lm()}}; the chart only receives ~80 fitted points, evenly
spaced across the x range.
}
\details{
Works on scatter charts and on line charts with a numeric or date x
axis. The fit pools every point into one curve, so multi-series
charts are refused - facet instead: build one chart per series level
and trend each. Repeated calls append, so a chart can carry a loess
curve and a straight line at once.
}
\examples{
f25 <- subset(pv_fiscal, year == 2025)
pv_scatter(f25, x = "resource_index", y = "equalization_chf") |>
  pv_trend("loess")
daily <- aggregate(revenue ~ date, pv_sales, sum)
pv_line(daily, x = "date", y = "revenue") |>
  pv_trend("lm", level = NA)
}
