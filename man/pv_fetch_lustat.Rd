% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{pv_fetch_lustat}
\alias{pv_fetch_lustat}
\title{Fetch a dataset from LUSTAT Statistik Luzern}
\usage{
pv_fetch_lustat(id, refresh = FALSE)
}
\arguments{
\item{id}{File name of the dataset on data.lustat.ch, with or without
the \code{.csv} suffix - e.g. \code{"fa-lu-ra"} (municipal fiscal equalization)
or \code{"grwahlen-lu"} (municipal council elections).}

\item{refresh}{Set to TRUE to bypass the cache and download again.}
}
\value{
A data frame; source and licence are attached as attributes and
printed on every fetch.
}
\description{
Downloads one of the open-government CSV files LUSTAT Statistik Luzern
publishes on \url{https://www.data.lustat.ch} (the resources its
opendata.swiss datasets point at) and parses it into a data frame.
Downloads are cached (see \code{\link[=pv_cache_status]{pv_cache_status()}}).
}
\details{
LUSTAT data is published under the opendata.swiss "OPEN BY ASK" terms:
free use with source citation ("Quelle: LUSTAT Statistik Luzern"), and
commercial use requires the data owner's permission. Every fetch prints
these terms and attaches them to the result (\code{pv_source}, \code{pv_licence},
\code{pv_url}).
}
\examples{
\dontrun{
fiscal <- pv_fetch_lustat("fa-lu-ra")
str(fiscal)
}
}
\seealso{
\code{\link[=pv_fetch_bfs]{pv_fetch_bfs()}}, \code{\link[=pv_fetch_eurostat]{pv_fetch_eurostat()}}, \code{\link[=pv_search_opendata]{pv_search_opendata()}},
\code{\link[=pv_cache_status]{pv_cache_status()}}
}
