% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-composition.R
\name{pv_donut}
\alias{pv_donut}
\title{Interactive D3 donut chart}
\usage{
pv_donut(
  data,
  category,
  value,
  inner_radius = 0.62,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 650,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{category}{Name of the category column — one slice per level, at
most 8. Beyond 8 a donut stops being readable; fold the smallest
categories into an \code{"Other"} bin first. Rows sharing a category are
summed into one slice.}

\item{value}{Name of the numeric column giving slice sizes. Values
must be non-negative.}

\item{inner_radius}{Size of the centre hole as a fraction of the outer
radius, from 0 to 0.85. \code{0} draws a classic pie.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Part-of-whole as a ring. Slices sweep in largest-first, slices worth 5\%
or more of the total carry their name and share directly, smaller ones
move to a legend, and the grand total sits in the hole. Hovering a
slice pops it outward and shows its exact value and share.
}
\examples{
seats <- aggregate(elected ~ party,
                   pv_elections[pv_elections$year == 2024, ], sum)
pv_donut(seats, category = "party", value = "elected",
         title = "Council seats by party, 2024")
}
