% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch.R
\name{pv_cache_status}
\alias{pv_cache_status}
\alias{pv_cache_clear}
\title{Inspect polyviz's open-data download cache}
\usage{
pv_cache_status()

pv_cache_clear()
}
\value{
\code{pv_cache_status()}: a data frame with one row per cached
download (\code{url}, \code{size_kb}, \code{fetched}, \code{file}), zero rows when the
cache is empty. \code{pv_cache_clear()}: the number of entries removed,
invisibly.
}
\description{
Downloads made by \code{\link[=pv_fetch_bfs]{pv_fetch_bfs()}}, \code{\link[=pv_fetch_lustat]{pv_fetch_lustat()}}, and
\code{\link[=pv_fetch_opendata]{pv_fetch_opendata()}} are kept under
\code{tools::R_user_dir("polyviz", "cache")} and reused on repeat calls (pass
\code{refresh = TRUE} to a fetcher to force a fresh download).
\code{pv_cache_status()} lists what is currently cached; \code{pv_cache_clear()}
deletes all of it.
}
\examples{
pv_cache_status()
}
