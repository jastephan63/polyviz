% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python.R
\name{pv_outliers}
\alias{pv_outliers}
\title{Flag outliers in a numeric vector}
\usage{
pv_outliers(
  x,
  method = c("iqr", "zscore"),
  k = 1.5,
  z = 3,
  engine = c("auto", "python", "r")
)
}
\arguments{
\item{x}{A numeric vector.}

\item{method}{\code{"iqr"} (default) or \code{"zscore"}.}

\item{k}{IQR multiplier (default 1.5).}

\item{z}{Z-score threshold (default 3).}

\item{engine}{\code{"auto"}, \code{"python"}, or \code{"r"}.}
}
\value{
A logical vector the same length as \code{x}.
}
\description{
\code{"iqr"} flags values outside \verb{[Q1 - k*IQR, Q3 + k*IQR]}; \code{"zscore"}
flags values more than \code{z} sample standard deviations from the mean.
Missing values are never flagged. Like \code{\link[=pv_profile]{pv_profile()}}, the work runs in
Python when available and in R otherwise, with identical results.
}
\examples{
x <- c(rnorm(50), 12)
which(pv_outliers(x, engine = "r"))
}
