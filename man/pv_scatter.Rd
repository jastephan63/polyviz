% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widgets.R
\name{pv_scatter}
\alias{pv_scatter}
\title{Interactive D3 scatter plot}
\usage{
pv_scatter(
  data,
  x,
  y,
  color = NULL,
  size = NULL,
  label = NULL,
  legend = "auto",
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 400,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame. Rows missing an x, y, or (when mapped) size
value are dropped with a warning.}

\item{x, y}{Names of numeric columns.}

\item{color}{Optional name of a categorical column (max 3 distinct
values keeps every pair distinguishable; more will error).}

\item{size}{Optional name of a numeric column mapped to point area.}

\item{label}{Optional name of a column shown in tooltips.}

\item{legend}{Show the colour legend row above the chart? \code{TRUE} always
shows it (when a \code{color} mapping exists), \code{FALSE} hides it; \code{"auto"}
(default) shows it exactly when a \code{color} mapping exists.}

\item{xlab, ylab}{Axis titles. \code{NULL} (default) uses the column names;
\code{NA} or \code{""} suppresses the title entirely; any other string
replaces it.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Scatter plot with per-point tooltips; optional colour (categorical) and
size (numeric) encodings.
}
\examples{
pv_scatter(mtcars, x = "wt", y = "mpg", size = "hp")
}
