% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-evolution.R
\name{pv_area}
\alias{pv_area}
\title{Interactive D3 area chart}
\usage{
pv_area(
  data,
  x,
  y,
  series = NULL,
  offset = c("stacked", "percent", "stream"),
  legend = "auto",
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame in long form: one row per series per x.}

\item{x}{Name of the x column — \code{Date}, numeric, or categorical.}

\item{y}{Name of the numeric value column.}

\item{series}{Optional name of the series column, one band per level.
At most 8 levels — beyond that, fold the small ones into an
\code{"Other"} level. Omit it for a single-series area.}

\item{offset}{\code{"stacked"} (default), \code{"percent"}, or \code{"stream"}.}

\item{legend}{\code{TRUE}, \code{FALSE}, or \code{"auto"} (default). \code{TRUE} always
draws the legend row (even for a single series), \code{FALSE} never draws
it, and \code{"auto"} draws it exactly when the chart has more than one
series.}

\item{xlab, ylab}{Axis titles. \code{NULL} (default) uses the column names;
\code{NA} or \code{""} suppresses a title; any other string replaces it. The
default y title only appears on \code{offset = "stacked"} (a percent axis
explains itself), but an explicit \code{ylab} shows there too. Streams
have no y axis, so they never draw a y title.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Stacked areas showing how a total and its parts move together over
time. \code{offset} picks the variant: \code{"stacked"} piles the raw values so
the outer edge is the total; \code{"percent"} normalises every x position to
100\%, turning the chart into composition over time; \code{"stream"} draws a
streamgraph — the stack is centred and smoothed, and the y axis
disappears because only the band widths mean anything. A crosshair
reads every series out of one tooltip at the hovered x position (with
each band's share in percent mode), and hovering a band lifts it out of
the stack. Series/x combinations without a row count as zero.
}
\examples{
agglo <- subset(pv_city_population,
                city \%in\% c("Luzern", "Emmen", "Kriens", "Horw", "Ebikon"))
pv_area(agglo, x = "year", y = "population", series = "city",
        title = "How the Lucerne agglomeration grew",
        source = "Source: Bundesamt für Statistik")
# The same data as a streamgraph:
pv_area(agglo, x = "year", y = "population", series = "city",
        offset = "stream")
}
