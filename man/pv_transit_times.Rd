% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtfs.R
\name{pv_transit_times}
\alias{pv_transit_times}
\title{Earliest arrival at every Swiss station}
\usage{
pv_transit_times(
  gtfs,
  origin,
  date,
  depart = "08:00",
  max_transfers = 4,
  max_minutes = 180
)
}
\arguments{
\item{gtfs}{A handle from \code{\link[=pv_fetch_gtfs]{pv_fetch_gtfs()}}.}

\item{origin}{The starting station, matched case-insensitively against
the feed's station names (e.g. \code{"Luzern"}); a station or platform id
works too. A miss aborts with the closest names; a name shared by
several stations aborts listing their ids.}

\item{date}{The service day, a \code{Date} or \code{"YYYY-MM-DD"} string.}

\item{depart}{Departure time at \code{origin}, \code{"HH:MM"} or \code{"HH:MM:SS"};
hours past 24 address the service day's own small hours, GTFS style.}

\item{max_transfers}{Maximum number of vehicle changes allowed.}

\item{max_minutes}{Travel-time horizon in minutes; stations not
reachable within it come back \code{NA}.}
}
\value{
A data frame with one row per station: \code{station} (name),
\code{stop_id} (the parent station's id), \code{lat}, \code{lon}, \code{minutes} (travel
time from \code{origin}, \code{NA} when unreachable within the limits), and
\code{transfers} (vehicle changes on that earliest journey). Rows are
sorted by \code{minutes}; the feed's source and licence ride along as
attributes.
}
\description{
Computes, for one departure from \code{origin} on one service day, the
earliest possible arrival time at every station in the feed - the raw
material of a travel-time (isochrone) map. Journeys may chain any
vehicles the timetable offers, changing where the feed's minimum
transfer times (or a two-minute default) allow, up to \code{max_transfers}
changes and \code{max_minutes} of travel.
}
\details{
The engine is a Connection Scan: DuckDB assembles the chosen day's
timetable - the services running that day (weekday flags plus
\code{calendar_dates} exceptions), their trips' consecutive stop pairs as
departure/arrival events, everything grouped to parent stations so
platforms count as one station - sorts it by departure, and caches it
as a Parquet file next to the extracted feed. R then sweeps the
connections once in departure order in vectorised blocks. On the
national feed the first query for a date spends about 20 seconds
building that day's connection table (roughly 3 million rows); the
sweep itself - a three-hour window pulls some 50 MB of connections
into R - takes a few seconds more. Later queries on the same date,
from any origin, skip the build and finish in a few seconds.

The details worth knowing:
\itemize{
\item Times past \code{"24:00"} are real GTFS times (the service day's own
after-midnight departures) and are included; the search does not
continue into the next day's services.
\item Station-to-station entries of \code{transfers.txt} (type 2) supply
minimum change times; where a station has no entry, changing
vehicles there costs two minutes. Stops flagged as no-boarding or
no-alighting are honoured.
\item Frequency-based trips (\code{frequencies.txt}, a handful of on-demand
services in the Swiss feed) are left out.
\item The answers are exactly what the feed says, closures included: a
station whose line is shut for engineering work that day really is
unreachable by rail, with the replacement buses arriving at their
own bus-stop station (e.g. \code{"Stans, Bahnhof"} rather than
\code{"Stans"}).
\item The scan keeps one label per station - the earliest arrival, with
the fewest changes achieving it - which honours \code{max_transfers}
without being a full multi-criteria search; in rare corners a
station reachable only by trading a later arrival for fewer changes
can come out \code{NA} or later than that trade would allow.
}
}
\examples{
\dontrun{
gtfs <- pv_fetch_gtfs()
reach <- pv_transit_times(gtfs, "Luzern", "2026-09-15")
head(reach, 20)
pv_gtfs_close(gtfs)
}
}
\seealso{
\code{\link[=pv_fetch_gtfs]{pv_fetch_gtfs()}}, \code{\link[=pv_gtfs_close]{pv_gtfs_close()}}
}
