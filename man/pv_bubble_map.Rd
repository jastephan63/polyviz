% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-geo.R
\name{pv_bubble_map}
\alias{pv_bubble_map}
\title{Interactive D3 bubble map}
\usage{
pv_bubble_map(
  data,
  lon,
  lat,
  size,
  color = NULL,
  label = NULL,
  map = "cantons",
  lakes = "auto",
  legend = "auto",
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 500,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame with one row per circle. Rows with a missing
longitude, latitude, or size are dropped with a warning, as are
points falling outside the map.}

\item{lon, lat}{Names of the numeric coordinate columns, in WGS84
degrees (longitude east, latitude north — \link{pv_city_coords} is the
bundled template).}

\item{size}{Name of the numeric column mapped to circle area. Must be
non-negative.}

\item{color}{Optional name of a categorical column (max 3 distinct
values keeps every pair distinguishable; more will error).}

\item{label}{Optional name of a column naming each point in tooltips.}

\item{map}{The base map: a layer name (\code{"cantons"}, the default;
\code{"districts"}; \code{"municipalities"}; \code{"lucerne"}), a FeatureCollection
list like \link{pv_swiss_cantons}, or an sf polygon object — the same
forms \code{\link[=pv_choropleth]{pv_choropleth()}} takes. Feature ids are not needed here; the
polygons are background.}

\item{lakes}{Draw the Swiss lakes beneath the circles? \code{TRUE},
\code{FALSE}, or \code{"auto"} (default: only on maps showing the whole of
Switzerland) — exactly as in \code{\link[=pv_choropleth]{pv_choropleth()}}.}

\item{legend}{Show the colour legend row above the chart? \code{TRUE}
always (when a \code{color} mapping exists), \code{FALSE} hides it; \code{"auto"}
(default) shows it exactly when a \code{color} mapping exists. The circle
size legend on the map is always drawn.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Circles on a map, sized by a value — the chart for quantities anchored
to places rather than regions: city populations, plant capacities,
event counts. The base map is drawn in a quiet neutral fill (with the
Swiss lakes over it, for country-wide layers) and each data row
becomes a circle at its longitude/latitude whose \emph{area} encodes the
value.
}
\details{
Circle area scales with the value (radius with its square root — the
honest encoding for sizes), up to a maximum radius that adapts to the
map's pixel size. Crowded maps get lighter fills so overlapping
circles stay readable, every circle wears a 2px surface-coloured ring,
and hovering one shows its name and exact value. A compact size legend
with two or three reference circles sits in the map's corner. An
optional \code{color} column maps up to three categories onto the palette,
with the usual legend row in the header.
}
\examples{
pop24 <- subset(pv_city_population, year == 2024)
cities <- merge(pv_city_coords, pop24, by = "city")
pv_bubble_map(cities, lon = "lon", lat = "lat", size = "population",
              label = "city", title = "Where urban Switzerland lives")
}
