% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-distribution.R
\name{pv_boxplot}
\alias{pv_boxplot}
\title{Interactive D3 boxplot}
\usage{
pv_boxplot(
  data,
  value,
  group = NULL,
  points = FALSE,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Name of the numeric column to summarise.}

\item{group}{Optional name of a grouping column (one box per level,
max 8). Omit it for a single box.}

\item{points}{Also show the raw values as jittered points behind each
box? Groups with more than 400 values are thinned to a sample of 400,
drawn with a fixed seed so the same data always shows the same
points. The box statistics always use every value.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Tukey boxplots of a numeric column, one box per group: the box spans
the quartiles, the 2px line is the median, whiskers reach the most
extreme values within 1.5 IQR of the box, and anything beyond is drawn
as an outlier dot. Hovering a box dims the others and reads out all
five numbers plus the group size.
}
\examples{
pv_boxplot(pv_fiscal, value = "resource_per_capita", group = "year",
           points = TRUE, title = "Municipal tax resources by year")
}
