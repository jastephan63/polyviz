% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-hierarchy.R
\name{pv_pack}
\alias{pv_pack}
\title{Zoomable D3 circle packing}
\usage{
pv_pack(
  data,
  levels,
  value,
  labels = "auto",
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame in long form: one row per leaf.}

\item{levels}{Character vector of column names, outermost grouping
first, defining the hierarchy. The first level takes the palette
colours, so it allows at most 8 distinct groups.}

\item{value}{Name of the numeric column summed within each circle.
Values must be non-negative — a circle's size is an area.}

\item{labels}{Write names inside the circles? \code{"auto"} (the default)
labels exactly the circles the text honestly fits into at the
current zoom level; \code{TRUE} squeezes labels into circles with about
20\% less room than that (the text must still fit at all — tiny
circles stay blank); \code{FALSE} draws no circle text, leaving the
names to the tooltip alone.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
A hierarchy as nested circles: each circle's area encodes its value,
its hue names its top-level branch, and deeper levels fade a step
toward the background. Click a circle to zoom into it (the classic
d3 zoomable pack fly-through); click the background, or the zoomed
circle itself, to zoom back out one level. Circles are labelled only
where the name honestly fits at the current zoom level; hovering any
circle shows its full path, value, and share of the total.
}
\examples{
# Summed over all 181 cities: what Swiss urban ground is made of.
pv_pack(pv_city_landuse, levels = c("group", "category"),
        value = "hectares", title = "Urban land use in Switzerland")
}
