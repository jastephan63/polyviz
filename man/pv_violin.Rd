% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/charts-distribution.R
\name{pv_violin}
\alias{pv_violin}
\title{Interactive D3 violin plot}
\usage{
pv_violin(
  data,
  value,
  group,
  box = TRUE,
  points = FALSE,
  xlab = NULL,
  ylab = NULL,
  title = NULL,
  subtitle = NULL,
  mode = "auto",
  duration = 600,
  source = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Name of the numeric column whose distribution is drawn.
Rows with a missing value or group are dropped with a warning.}

\item{group}{Name of the grouping column (one violin per level, max 8;
every level needs at least 2 non-missing values for a density).}

\item{box}{Overlay a slim Tukey box-and-whisker inside each violin?
\code{TRUE} always draws it, \code{FALSE} never does; \code{"auto"} draws it —
same as \code{TRUE} today, but reserved for a future data-driven rule.
The box marks the quartiles and median so the shapes stay anchored
to exact numbers.}

\item{points}{Also show the raw values as jittered points inside each
violin? The dots stay within the silhouette — each one's sideways
room is the violin's width at its own value — so the cloud traces
the same shape the outline does. \code{TRUE} always draws them (groups
with more than 400 values are thinned to a fixed-seed sample of
400, exactly like \code{\link[=pv_boxplot]{pv_boxplot()}}), \code{FALSE} (the default) never
does, and \code{"auto"} draws them only while every group holds at most
200 values — few enough that the dots inside the narrow shape
still read individually.}

\item{xlab, ylab}{Axis titles. \code{NULL} (the default) uses the \code{group}
column name for x and the \code{value} column name for y; \code{NA} or \code{""}
suppresses the title; any other string replaces it.}

\item{title, subtitle}{Optional chart heading text.}

\item{mode}{\code{"auto"} (default: follow the viewer's light/dark setting),
\code{"light"}, or \code{"dark"}.}

\item{duration}{Entrance transition length in ms.}

\item{source}{Optional source/credit line, shown small and grey at the
bottom left — e.g. \code{"Source: Bundesamt für Statistik"}.}

\item{width, height, elementId}{Standard htmlwidgets sizing arguments.}
}
\value{
An htmlwidget.
}
\description{
Mirrored kernel density curves, one violin per group on a shared value
axis — the boxplot's five numbers replaced by the distribution's whole
shape, so bimodality and skew stay visible. Each violin reaches 85\% of
its band at its own mode, so all the shapes read equally wide and only
their outline differs. Hovering a violin dims the others and reads out
the group's size, median, and quartiles.
}
\examples{
pv_violin(pv_fiscal, value = "resource_per_capita", group = "year",
          title = "Municipal tax resources by year")
}
